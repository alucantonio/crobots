# CROBOTS trace outputs (CSV trajectories / state dumps from the `-t` flag)
*.csv
traces/

# compiler object files
*.o

# Python bytecode caches
__pycache__/

# sweep-generated variant robots (regenerable: source + manifest + config)
tools/sweep/variants/

# ...but keep the sweep result tables
!tools/sweep/results/*.csv
